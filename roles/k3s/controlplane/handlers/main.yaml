---

# - name: Reboot host and wait for it to restart
#   reboot:
#     msg: "Reboot initiated by Ansible"
#     connect_timeout: 5
#     reboot_timeout: 600
#     pre_reboot_delay: 0
#     post_reboot_delay: 30
#     test_command: whoami

# - name: restart_k3s.service
#   systemd:
#     name: k3s
#     daemon_reload: yes
#     state: restarted
#     enabled: yes
#   ignore_errors: yes

- name: restart_k3s.service
  command: systemctl restart k3s
  register: k3s_started
  retries: 3
  delay: 3
  until: k3s_started.rc == 0
